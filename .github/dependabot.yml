# Dependabot configuration.
#
# Dependabot opens automated PRs to keep dependencies current.
# Each update group is checked weekly on Monday mornings (UTC).
#
# References:
#   https://docs.github.com/en/code-security/dependabot/dependabot-version-updates/configuration-options-for-the-dependabot.yml-file

version: 2

updates:
  # ── GitHub Actions ──────────────────────────────────────────────────────────
  # Keeps action versions in .github/workflows/*.yml up to date.
  # Dependabot will also propose SHA-pinned updates, which is the recommended
  # hardening step for supply-chain security (SLSA / OpenSSF Scorecards).
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "06:00"
      timezone: "UTC"
    labels:
      - "dependencies"
      - "github-actions"

  # ── Docker ──────────────────────────────────────────────────────────────────
  # Tracks the kcov/kcov base image pinned directly in the FROM line.
  # Note: kcov/kcov:latest currently tracks a pre-release; Dependabot will
  # propose stable version bumps as new releases are tagged on Docker Hub.
  - package-ecosystem: "docker"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "06:00"
      timezone: "UTC"
    labels:
      - "dependencies"
      - "docker"
